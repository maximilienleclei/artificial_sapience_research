# one_shot

One-shot work starts from a large user-authored prompt/spec and rewrites the implementation in one pass with minimal technical debt as the primary goal.

When modifications reveal underspecified intent, prefer updating the prompt/spec and re-one-shotting over accumulating patchwork.
