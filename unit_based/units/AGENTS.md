# AGENTS.md

1. Each numbered unit folder is a self-contained archive.
2. Keep runnable code under `code/`.
3. Put generated plots and plot-adjacent metrics under `plot/`.
4. Do not create a `plot/` folder for units that do not produce plots.
5. Use each unit's `STATUS.md` as the rerun and verification record.
